# nfproducer-template-bun

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```

*or*

```bash
docker compose up
```

To generate a uuid for the source:

```bash
bun run uuid
```
